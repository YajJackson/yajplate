import { HomePage } from '@pages/home'
import { LoginPage } from '@pages/login'

const routes = [
    {
        path: '/',
        component: HomePage,
    },
    {
        path: '/login',
        component: LoginPage,
    },
]

export default routes
